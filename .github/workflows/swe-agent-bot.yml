name: swe-agent-bot

on:
  issues:
    types: [labeled]

jobs:
  run:
    if: contains(github.event.issue.labels.*.name, 'bot:fix')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          fetch-depth: 0
          token: ${{ secrets.SWE_AGENT_GH_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git curl
      
      - name: Clone and Install SWE-agent
        run: |
          python -m pip install --upgrade pip
          git clone --depth 1 https://github.com/SWE-agent/SWE-agent.git /tmp/swe-agent
          cd /tmp/swe-agent
          pip install -e .
      
      - name: Run SWE-agent
        env:
          GITHUB_TOKEN: ${{ secrets.SWE_AGENT_GH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TOGETHER_API_KEY: ${{ secrets.TOGETHER_API_KEY }}
        run: |
          cd /tmp/swe-agent
          sweagent run \
            --config config/default.yaml \
            --config ${{ github.workspace }}/.github/swe-agent.yaml \
            --agent.model.name="anthropic/claude-sonnet-4.5" \
            --agent.model.api_base="https://claude.nnppn.cn/api" \
            --agent.model.parse_function="thought_action" \
            --agent.model.max_input_token=200000 \
            --env.repo.github_url=https://github.com/${{ github.repository }}.git \
            --problem_statement.github_url=https://github.com/${{ github.repository }}/issues/${{ github.event.issue.number }}

