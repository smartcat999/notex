name: swe-agent-bot

on:
  issues:
    types: [labeled]

jobs:
  run:
    if: contains(github.event.issue.labels.*.name, 'bot:fix')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    env:
      # 统一分支命名，避免并发冲突
      SWE_BRANCH: swe-agent/issue-${{ github.event.issue.number }}-${{ github.run_id }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          fetch-depth: 0
          token: ${{ secrets.SWE_AGENT_GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git

      - name: Install SWE-agent
        run: |
          python -m pip install --upgrade pip
          # 需要克隆完整仓库以包含 config 目录
          git clone --depth 1 https://github.com/SWE-agent/SWE-agent.git /tmp/swe-agent
          cd /tmp/swe-agent
          pip install -e .
          # 确保 config 目录存在
          ls -la config/ || echo "Warning: config directory not found"

      - name: Configure git identity for PR commits
        run: |
          git config user.name "swe-agent-bot"
          git config user.email "swe-agent-bot@users.noreply.github.com"

       # 关键：让 SWE-agent 修改"当前工作区"的代码
      - name: Run SWE-agent (edit local workspace)
        env:
          GITHUB_TOKEN: ${{ secrets.SWE_AGENT_GH_TOKEN }}
          # 二选一：如果你走 Anthropic 原生 API，则用这个
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # 二选一：如果你走 OpenAI 兼容代理(例如 LiteLLM/OpenRouter/自建网关)，可用 OPENAI_API_KEY
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
           cd /tmp/swe-agent
           sweagent run \
             --config config/default.yaml \
             --config ${{ github.workspace }}/.github/swe-agent.yaml \
             --env.repo.type=local \
             --env.repo.path=${{ github.workspace }} \
             --problem_statement.github_url=https://github.com/${{ github.repository }}/issues/${{ github.event.issue.number }}

      - name: Create pull request if changes exist
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.SWE_AGENT_GH_TOKEN }}
          branch: ${{ env.SWE_BRANCH }}
          base: ${{ github.event.repository.default_branch }}
          commit-message: "[SWE-Agent] Auto fix for #${{ github.event.issue.number }}"
          title: "[SWE-Agent] Auto fix for #${{ github.event.issue.number }}: ${{ github.event.issue.title }}"
          body: |
            This PR was automatically generated by SWE-Agent 🤖

            **Issue:** #${{ github.event.issue.number }}
            **Actor:** ${{ github.actor }}
            **Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            > If something looks off, close this PR and remove the `bot:fix` label.
          labels: automated, ai-fix
          assignees: ${{ github.actor }}
          draft: false

      - name: Comment on issue with PR link (via gh)
        if: steps.cpr.outputs.pull-request-number != ''
        env:
          GH_TOKEN: ${{ secrets.SWE_AGENT_GH_TOKEN }}
        run: |
          gh issue comment ${{ github.event.issue.number }} \
            --body "✅ Created PR #${{ steps.cpr.outputs.pull-request-number }} from branch \`${{ env.SWE_BRANCH }}\`.

            Thanks for the label \`bot:fix\` — review the changes when you have a moment."
