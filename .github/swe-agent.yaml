# SWE-agent Configuration
# 参考: https://swe-agent.com/docs/configuration

# Agent configuration
agent:
  # 模型配置 - 可以选择 claude-sonnet-4, gpt-4o, 或其他支持的模型
  model:
    name: "claude-sonnet-4.5"  # 或 "gpt-4o", "gpt-4-turbo" 等
    provider: "anthropic"  # 或 "openai", "together" 等
    temperature: 0.0
    # 如果使用本地模型或自定义 API 端点，取消下面的注释：
    api_base: "https://claude.nnppn.cn/api"
  
  # 成本追踪（如果使用自定义模型或代理，可禁用）
  cost_tracking:
    enabled: true
    # 自定义价目表（可选）
    # pricing:
    #   claude-sonnet-4-20250514:
    #     prompt: 0.003
    #     completion: 0.015

# 执行环境配置
environment:
  # 使用 Docker 作为执行后端（推荐，提供隔离环境）
  execution_type: "docker"
  # 如果不想使用 Docker，可以改为 "local"
  # execution_type: "local"
  
  # Docker 配置
  docker:
    image: "sweagent/environment:ubuntu-22.04"
    # 可选：自定义 Docker 镜像或配置

# 工具配置
tools:
  # 启用的工具列表
  enabled:
    - "file_editor"
    - "shell"
    - "search_replace"
    - "read_file"
    - "grep"
    - "search"
  
  # 工具特定配置
  file_editor:
    max_file_size_mb: 10
  
  shell:
    timeout_seconds: 30
    max_command_length: 1000

# 任务配置
task:
  # 最大步数限制
  max_iterations: 50
  
  # 是否自动提交更改
  auto_commit: true
  
  # 提交消息模板
  commit_message_template: "Fix: {task_description}"

# 输出配置
output:
  # 是否保存执行轨迹
  save_trajectory: true
  trajectory_dir: ".swe-agent-trajectories"
  
  # 日志级别
  log_level: "INFO"

# 仓库特定配置
repository:
  # 分支命名模式
  branch_prefix: "swe-agent"
  
  # PR 标题模板
  pr_title_template: "🤖 Auto-fix: {issue_title}"
  
  # PR 描述模板
  pr_body_template: |
    This PR was automatically created by SWE-agent.
    
    **Issue**: #{issue_number}
    **Task**: {task_description}
    
    ---
    
    Generated by [SWE-agent](https://github.com/SWE-agent/SWE-agent)

